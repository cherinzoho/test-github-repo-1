[project]
name            = "robot_arm_py"
version         = "0.1.0"
description     = "Python nodes for robot arm"
requires-python = ">=3.10"
dependencies    = ["rclpy"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
